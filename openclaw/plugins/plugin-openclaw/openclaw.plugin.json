{
  "id": "plugin-openclaw",
  "name": "RelAI Marketplace",
  "description": "Browse and call paid APIs on the RelAI marketplace. Agent key setup, API discovery, and metered API calls with automatic x402 payment.",
  "version": "0.2.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string",
        "description": "Base URL of the RelAI platform"
      },
      "x402Domain": {
        "type": "string",
        "description": "Root domain for subdomain-routed paid calls (e.g. x402.fi). Calls go to {subdomain}.{x402Domain}{path} when the API record has a subdomain, falling back to {baseUrl}/relay/{apiId}{path}."
      },
      "requestTimeoutMs": {
        "type": "number",
        "description": "HTTP request timeout in milliseconds"
      }
    }
  },
  "uiHints": {
    "baseUrl": {
      "label": "RelAI URL",
      "placeholder": "https://relai.fi"
    },
    "x402Domain": {
      "label": "x402 Domain",
      "placeholder": "x402.fi"
    },
    "requestTimeoutMs": {
      "label": "Request timeout (ms)",
      "placeholder": "15000"
    }
  }
}
